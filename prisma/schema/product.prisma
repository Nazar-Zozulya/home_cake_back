model Product { 
    id           Int    @id @default(autoincrement())
    image        String
    name         String
    price        Float
    composition  String
    weight       Int
    kilocalories Int
}